let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
//resultado = Math.ceil(2.2); // cell siempre redonda arriba
//resultado = Math.floor(2.9); // floor redondea abaj

console.log(resultado);